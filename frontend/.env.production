# VITE_API_URL is intentionally left unset in production.
# vercel.json rewrites /api/* → the Render backend, so axios uses the relative /api fallback.
# Setting this to the Render URL directly would bypass the proxy and cause CORS errors.
VITE_WS_URL=wss://distributed-job-scheduler-backend-58c9.onrender.com/ws